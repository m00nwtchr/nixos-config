[colors]
background = {{ background | hexa | strip }}
text = "{{ foreground | strip }}FF"
match = "{{ color12 | strip }}FF"
selection-match = "{{ color12 | strip }}FF"
selection = "{{ color6 | strip }}FF"
selection-text = "{{ foreground | strip }}FF"
border = {{ background | hexa | strip }}

prompt = "{{ color12 | strip }}FF"
counter = "{{ color12 | strip }}FF"
